<!-- Colors.stories.mdx -->

import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';

<Meta title="Colors" />

# Theme Colours

The default theme colours

<ColorPalette>
  <ColorItem
    title="theme.color.grey"
    subtitle="Some of the greys"
    colors={{
      50: '#fafafa',
      100: '#f5f5f5',
      200: '#e5e5e5',
      300: '#d4d4d4',
      400: '#a3a3a3',
      500: '#737373',
      600: '#525252',
      700: '#404040',
      800: '#262626',
      900: '#171717',
    }}
  />
  <ColorItem
    title="theme.colors.primary"
    subtitle="Primary"
    colors={{ light: '#115DD0', main: '#0D47A1', dark: '#0B3B84', }}
  />
  <ColorItem
    title="theme.color.secondary"
    subtitle="Secondary"
    colors={{ light: '#2CB5A7', main: '#21897E', dark: '#1C736A', }}
  />
  <ColorItem
    title="theme.color.tertiary"
    subtitle="Tertiary"
    colors={{ light: '#C397B0', main: '#AF7595', dark: '#A66488', }}
  /> 
  <ColorItem
    title="theme.color.info"
    subtitle="Info"
    colors={{ light: '#48C3D5', main: '#2DB3C8', dark: '#2695A6', }}
  />
  <ColorItem
    title="theme.color.success"
    subtitle="Success"
    colors={{ light: '#20D59C', main: '#19A97B', dark: '#158E68', }}
  />
  <ColorItem
    title="theme.color.error"
    subtitle="Error"
    colors={{ light: '#E0526c', main: '#DA2C4C', dark: '#BF223E', }}
  />

  <ColorItem
    title="theme.color.warning"
    subtitle="Warning"
    colors={{ light: '#FDCA5E', main: '#FCBD36', dark: '#FBB00E', }}
  />
  <ColorItem
    title="theme.color.singleTone"
    subtitle="SingleTone"
    colors={{
      white: '#ffffff',
      black: '#000000',
      lightText: '#ffffff',
      darkText: '#27272a',
    }}
  />
</ColorPalette>

# Theme colours and editing

Theme color object is build as below

```typescript
defaultTheme = {
    colors:{
      primary:{
        light?: string;
        main?: string;
        dark?: string;
      },
      secondary:{
        light?: string;
        main?: string;
        dark?: string;
      },
      tertiary:{
        light?: string;
        main?: string;
        dark?: string;
      },
      info:{
        light?: string;
        main?: string;
        dark?: string;
      },
      success:{
        light?: string;
        main?: string;
        dark?: string;
      },
      error:{
        light?: string;
        main?: string;
        dark?: string;
      },
      warning:{
        light?: string;
        main?: string;
        dark?: string;
      },
      singleTone:{
        white?: string;
        black?: string;
        lightText?: string;
        darkText?: string;
      },
      grey:{
        50?: string;
        100?: string;
        200?: string;
        300?: string;
        400?: string;
        500?: string;
        600?: string;
        700?: string;
        800?: string;
        900?: string;
      }
    }
  }
```

To edit the colours import the ComponentsThemeProvider wrap your app with it and add your new theme as a props,
the new colours will be merged with the default theme only replacing the colours you've defined in your new theme

In the below example the primary colours have been edited

```jsx
function App() {
  const theme = {
    colors: {
      primary: {
        light: 'lightred',
        main: 'red',
        dark: 'darkred',
      },
    },
  };

  return (
    <div>
      <ComponentsThemeProvider theme="theme">
        ... app components
      </ComponentsThemeProvider>
    </div>
  );
}
```
